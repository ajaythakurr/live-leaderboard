services:

  backend:
    build: ./backend
    ports:
      - "5050:5050"
    env_file:
      - ./backend/.env
    depends_on:
      - redis
      - mongo

  mongo:
    image: mongo

    ports:
      - "27017:27017"

  redis:
    image: redis

    ports:
      - "6379:6379"